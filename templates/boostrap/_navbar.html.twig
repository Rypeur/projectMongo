<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_index_index') }}">Site de Vente de Gaming</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
                aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_index_index') }}">Home
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact_index') }}">Contact</a>
                </li>
                {% if app.user %}
                    <li class="nav-item"><div class="btn-group" role="group">
                            <a class="nav-link">{{ app.user.email }}</a>
                            <div class="btn-group" role="group">
                                <a id="btnGroupDrop1" class="nav-link dropdown-toggle" data-toggle="dropdown"
                                   aria-haspopup="true" aria-expanded="false"></a>
                                <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                    <a class="dropdown-item" href="{{ path('app_user_account') }}">Mon compte</a>
                                    {% if is_granted('ROLE_ADMIN') %}
                                        <a class="dropdown-item" href="{{ path('app_admin_index') }}">Dashboard</a>
                                        <a class="dropdown-item" href="{{ path('app_admin_administration') }}">Administration</a>
                                    {% else %}
                                        <a class="dropdown-item" href="{{ path('app_user_index') }}">Dashboard</a>
                                    {% endif %}

                                    <a class="dropdown-item" href="{{path('app_user_viewbuy') }}">Mes Commandes</a>
                                    <a class="dropdown-item" href="{{ path('app_user_viewsell') }}">Mes Ventes</a>
                                    <a class="dropdown-item" href="{{path('app_security_logout') }}">Déconnection</a>
                                </div>
                            </div>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_security_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_security_signup') }}">Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>
